import { CheckboxStringValueDirective } from './checkbox-string-value.directive';

describe('CheckboxStringValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxStringValueDirective();
    expect(directive).toBeTruthy();
  });
});
