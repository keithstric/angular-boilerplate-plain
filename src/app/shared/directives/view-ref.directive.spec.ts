import { ViewRefDirective } from './view-ref.directive';

describe('ViewRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewRefDirective();
    expect(directive).toBeTruthy();
  });
});
